name: Test Python package

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/reusable-test.yaml
    with:
      commit: ${{ github.sha }}
